namespace Aufgabe2_3_2 {
/*
for (let i: number = 0; i < beineAuswahl.length; i++) {
    let a: number = i;
    let divBeine: HTMLElement = document.getElementById("beineDiv" + String(a));
    let bildBeine: HTMLImageElement = document.createElement("img");
    bildBeine.setAttribute("scr", beineAuswahl[i].quelle);
    bildBeine.classList.add("Bilder");
    bildBeine.dataset.name = beineAuswahl[i].name;
    divBeine.appendChild(bildBeine);
    bildBeine.addEventListener("click", linkStartseite);
    function linkStartseite(): void {
        let auswahlBeine: string = koerperAuswahl[i].quelle;
        let myJSON3: string = JSON.stringify(auswahlBeine);
        localStorage.setItem("beineAuswahlStorage", myJSON3);
        document.location.href = "index.html";
    }
}

*/


}